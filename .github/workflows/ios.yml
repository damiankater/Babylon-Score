name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: Build Babylon Score IPA
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Install MAUI workload
        run: dotnet workload install maui

      - name: Patch csproj for .NET 9 iOS build
        run: |
          sed -i '' 's/net10.0-ios/net9.0-ios/g' MauiApp1.csproj
          sed -i '' 's/net10.0-android/net9.0-android/g' MauiApp1.csproj

      - name: Restore dependencies
        run: dotnet restore

      - name: Build iOS Release
        run: |
          dotnet publish -f net9.0-ios -c Release \
            /p:RuntimeIdentifier=ios-arm64 \
            /p:CodesignKey='' \
            /p:CodesignProvision=''

      - name: Create IPA from .app bundle
        run: |
          APP_PATH=$(find . -name "*.app" -type d | head -1)
          echo "Found app: $APP_PATH"
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -qq -r -9 BabylonScore.ipa Payload/

      - name: Upload IPA artifact
        uses: actions/upload-artifact@v4
        with:
          name: BabylonScore-iOS
          path: BabylonScore.ipa
          retention-days: 30
